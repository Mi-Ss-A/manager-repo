spring:
  application:
    name: configrepo
  config:
    import: "configserver:http://localhost:9000"  # localhost -> config-server
  cloud:
    config:
      fail-fast: false
      uri: http://localhost:9000    # ??: ??? URI ??
  profiles:
    active: dev